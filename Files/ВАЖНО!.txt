Изглежда, в задачата Binary.cpp има някаква нередност. При компилация
с CodeBlocks всичко е наред, но при пускане с DevC++ извежда и
допълнителни неща, каквито не би трябвало да има. Ще се опитам да
видя къде е проблемът.

Също така, reinterpret_cast не е задължителен, може просто
съответно (const char*) и (char*).